/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Add your Remote Image domains here :
    domains: ["website-image-1.s3.amazonaws.com"],
  },
};

export default nextConfig;
